# Kopieren nach .env und Werte anpassen.

# PostgreSQL (lokal: docker compose up -d)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/grabstein?schema=public"

# SMTP (später, Phase 2)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
